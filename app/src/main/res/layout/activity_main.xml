<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
    <EditText
        android:inputType="number"
        android:layout_width="match_parent"
        android:ems="10"
        android:layout_height="wrap_content"
        android:id="@+id/etLoan"
        android:hint="စုစုပေါင်းချေးငွေ"/>
    <EditText
        android:inputType="number"
        android:layout_width="match_parent"
        android:ems="10"
        android:layout_height="wrap_content"
        android:layout_below="@id/etLoan"
        android:id="@+id/etInterest"
        android:hint="အတိုးနှုန်း"/>
    <EditText
        android:inputType="number"
        android:layout_width="match_parent"
        android:ems="10"
        android:layout_height="wrap_content"
        android:layout_below="@id/etInterest"
        android:id="@+id/etPayment"
        android:hint="လစဉ်သွင်းရမည့်ငွေ"/>
    <Button
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="တွက်ပါ"
        android:layout_below="@id/etPayment"
        android:layout_centerHorizontal="true"
        android:id="@+id/calculate"
        android:onClick="onClick"/>
    <ScrollView
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_below="@id/calculate"
        android:layout_alignParentBottom="true">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:id="@+id/tv"/>
    </ScrollView>
</RelativeLayout>